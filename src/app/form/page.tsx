import { AppShell } from '@/shared/ui/AppShell';
import { FormPage } from '../../domains/form/FormPage';

export default function Page() {
  return (
    <AppShell>
      <FormPage />
    </AppShell>
  );
}
